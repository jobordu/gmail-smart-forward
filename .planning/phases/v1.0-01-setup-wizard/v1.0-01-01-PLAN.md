---
phase: v1.0-01-setup-wizard
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - jest.config.js
  - scripts/lib/__tests__/env-parser.test.js
  - scripts/lib/__tests__/validators.test.js
  - scripts/lib/__tests__/keyword-gen.test.js
  - package.json
autonomous: true
requirements:
  - SETUP-07
  - SETUP-08
  - SETUP-02
  - SETUP-04
  - SETUP-05
  - SETUP-03
  - REL-01
  - REL-03

must_haves:
  truths:
    - "`npx jest` exits 0 (all stubs pass — tests exist but no source yet)"
    - "Test files exist at `scripts/lib/__tests__/env-parser.test.js`, `validators.test.js`, `keyword-gen.test.js`"
    - "`jest` is listed in `devDependencies` in `package.json`"
    - "Jest config file `jest.config.js` exists at project root"
  artifacts:
    - path: "jest.config.js"
      provides: "Jest configuration pointing at scripts/lib/__tests__/"
    - path: "scripts/lib/__tests__/env-parser.test.js"
      provides: "Test stubs for env load/write/patch operations"
    - path: "scripts/lib/__tests__/validators.test.js"
      provides: "Test stubs for email and filetype validation"
    - path: "scripts/lib/__tests__/keyword-gen.test.js"
      provides: "Test stubs for keyword generation by language"
  key_links:
    - from: "jest.config.js"
      to: "scripts/lib/__tests__/"
      via: "testMatch pattern"
      pattern: "__tests__"
---

<objective>
Install Jest and create test scaffolding (stub test files) for the three core library modules that the Setup Wizard will depend on. No source implementation yet — only test files with `describe`/`it.todo` stubs.

Purpose: Wave 0 scaffolding ensures the Nyquist validation requirement is met — tests must exist before implementation begins. Executors pick up Wave 1 plans immediately after.

Output: `jest.config.js`, three stub test files under `scripts/lib/__tests__/`, and `jest` added to `devDependencies`.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.0-01-setup-wizard/v1.0-01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Jest and create jest.config.js</name>
  <files>
    package.json
    jest.config.js
  </files>
  <action>
1. Run: `npm install --save-dev jest`
   This adds `jest` to `devDependencies` in `package.json`.

2. Create `jest.config.js` at the project root with this content:

```js
// jest.config.js
/** @type {import('jest').Config} */
module.exports = {
  testEnvironment: 'node',
  testMatch: ['**/scripts/lib/__tests__/**/*.test.js'],
  verbose: true,
};
```

3. Add a `test` script to `package.json` scripts (do NOT overwrite any existing scripts):
```json
"test": "jest"
```

Do NOT add `"type": "module"` to package.json — the project uses CommonJS (`require()`).

Confirm `jest` appears in `devDependencies` after install:
```bash
node -e "const p = require('./package.json'); console.log(p.devDependencies.jest ? 'OK' : 'MISSING')"
```
  </action>
  <verify>
    `npx jest --listTests` — should return 0 test files (no test files exist yet, should not error)
    `node -e "require('./jest.config.js')"` — must not throw
  </verify>
  <done>
    `jest.config.js` exists at root.
    `jest` is in `devDependencies` in `package.json`.
    `npx jest --listTests` runs without error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create stub test files for the three library modules</name>
  <files>
    scripts/lib/__tests__/env-parser.test.js
    scripts/lib/__tests__/validators.test.js
    scripts/lib/__tests__/keyword-gen.test.js
  </files>
  <action>
Create the `scripts/lib/__tests__/` directory and three stub test files. These are stubs — `it.todo()` entries only. No source files imported yet (stubs skip import until Wave 1 creates them).

**`scripts/lib/__tests__/env-parser.test.js`:**
```js
// scripts/lib/__tests__/env-parser.test.js
// Stubs for env-parser.js — populated after Wave 1 implements the module

describe('env-parser: load()', () => {
  it.todo('returns empty object when .env does not exist');
  it.todo('returns key/value pairs from a valid .env file');
  it.todo('ignores comment lines starting with #');
  it.todo('ignores blank lines');
});

describe('env-parser: write()', () => {
  it.todo('writes a new .env file when none exists');
  it.todo('updates existing key values without losing comments');
  it.todo('appends new keys not present in the existing file');
  it.todo('writes atomically via .env.tmp then rename (no partial writes)');
  it.todo('does not overwrite .env if the tmp write throws');
});
```

**`scripts/lib/__tests__/validators.test.js`:**
```js
// scripts/lib/__tests__/validators.test.js
// Stubs for validators.js — populated after Wave 1 implements the module

describe('validators: isValidEmail()', () => {
  it.todo('returns true for a valid email address');
  it.todo('returns false for an address missing @');
  it.todo('returns false for an address missing domain');
  it.todo('returns false for an empty string');
});

describe('validators: isValidEmailList()', () => {
  it.todo('returns true for a comma-separated list of valid emails');
  it.todo('returns false if any email in the list is invalid');
  it.todo('handles extra whitespace around commas');
  it.todo('returns true for an empty string (optional field)');
});

describe('validators: isValidFileTypeList()', () => {
  it.todo('returns true for "pdf"');
  it.todo('returns true for "pdf,xlsx"');
  it.todo('returns false for an empty string');
  it.todo('returns false for values with dots (e.g., ".pdf")');
});
```

**`scripts/lib/__tests__/keyword-gen.test.js`:**
```js
// scripts/lib/__tests__/keyword-gen.test.js
// Stubs for keyword-gen.js — populated after Wave 1 implements the module

describe('keyword-gen: generate()', () => {
  it.todo('returns SUBJECT_KEYWORDS and ATTACHMENT_FILENAME_KEYWORDS strings');
  it.todo('generates English keywords when "en" is in the language list');
  it.todo('generates French keywords when "fr" is in the language list');
  it.todo('combines keywords from multiple languages');
  it.todo('deduplicates keywords that appear in multiple language sets');
  it.todo('returns empty strings for an empty language array');
  it.todo('handles unsupported language codes gracefully (skips them)');
});
```
  </action>
  <verify>
    `npx jest --bail` — should report "Test Suites: 3 passed, 3 total" with all todos shown (todos pass by default in Jest)
  </verify>
  <done>
    All three test files exist under `scripts/lib/__tests__/`.
    `npx jest` exits 0 with 3 test suites, all `todo` items shown.
    No import errors (stubs do not import source files).
  </done>
</task>

</tasks>

<verification>
- `npx jest` runs and exits 0
- Three test suite files exist at `scripts/lib/__tests__/`
- `jest` is in `devDependencies`
- No changes to `src/` directory (Apps Script files untouched)
- No changes to existing `scripts/build-env.js`
- `package.json` now has a `"test": "jest"` script
</verification>

<success_criteria>
Wave 0 complete: jest installed, config present, 3 stub test files exist, `npx jest` passes with all todos.
</success_criteria>

<output>
After completion, create `.planning/phases/v1.0-01-setup-wizard/v1.0-01-01-SUMMARY.md`
</output>
